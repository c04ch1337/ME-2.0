TWILIO_ACCOUNT_SID=your_sid
TWILIO_AUTH_TOKEN=your_token
TWILIO_PHONE_NUMBER=+1234567890
OLLAMA_MODEL=llama3
MEM0_USER_ID=me
DREAMLIT_API_KEY=your_key
GOOGLE_CLIENT_ID=your_id
# GPU Configuration
CUDA_VISIBLE_DEVICES=2,3

# Application Settings
LOG_LEVEL=INFO
MODEL_PATH=/app/models
DATA_PATH=/app/data
APP_ENV=development
# OpenAI (required for mem0 by default; also used by bolna via OpenAI/LiteLLM)
OPENAI_API_KEY=your_openai_api_key
# Optional: override base URL for OpenAI-compatible providers (Azure/OpenRouter/self-hosted proxy)
OPENAI_BASE_URL=
# To avoid using OpenAI for embeddings in mem0, you can use a local provider (uncomment to switch)
# MEM0_EMBEDDINGS_PROVIDER=fastembed